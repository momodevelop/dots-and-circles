#include "../../code/momo.h"

static inline b8
U8Str_Format(String_Buffer* Dest, String Format, ...) {
    
}

int main() {
    u8 Buffer[256];
    String_Buffer Str = U8Str_CreateFromBuffer(Buffer);
}